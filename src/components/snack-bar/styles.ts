import styled from "styled-components";
import transition from "styled-transition-group";

const Container = styled.div``;

export const AnimContainer = transition(Container)``;

export const CloseBtn = styled.button``;
